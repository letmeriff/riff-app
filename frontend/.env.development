# Development Environment Variables

# Canvas component has been fully refactored
# The feature flags below have been removed since they are no longer needed
# All components now use the optimized implementation by default

# API URL for development
REACT_APP_API_URL=http://localhost:3001 